{
  "name": "cua-operator-console",
  "private": true,
  "description": "TypeScript operator console for human-in-the-loop escalations",
  "scripts": {
    "build": "esbuild src/console.ts --bundle --outfile=dist/console.js --target=es2020"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
