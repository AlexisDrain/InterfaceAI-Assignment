# Required only for discovery runs (the LLM-driven path).
# Deterministic replay never calls the model and works without this.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
# CUA_MODEL=claude-opus-5
# CUA_TARGET_BASE_URL=http://127.0.0.1:8300
